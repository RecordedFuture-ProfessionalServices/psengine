name: Format

on:
  workflow_dispatch:
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Run ruff check
        uses: astral-sh/ruff-action@v3
        with:
          args: "format --check --diff"
